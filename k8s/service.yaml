apiVersion: v1
kind: Service
metadata:
  name: lildeploy
  labels:
    app: lildeploy
spec:
  type: ClusterIP
  selector:
    app: lildeploy
  ports:
    - name: http
      port: 80
      targetPort: 8000
